info:
  name: show
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8050/plates-category/3
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
