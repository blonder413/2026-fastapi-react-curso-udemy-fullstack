info:
  name: create
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8050/plates-category
  body:
    type: json
    data: |-
      {
        "name": "Plate 3"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
