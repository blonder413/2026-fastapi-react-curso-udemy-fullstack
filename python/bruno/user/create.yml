info:
  name: create
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8050/user
  body:
    type: json
    data: |-
      {
        "name": "Ethan Winter",
        "profile_id": 1,
        "email": "ewinters@email.org",
        "password": "password",
        "token": "abc123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
