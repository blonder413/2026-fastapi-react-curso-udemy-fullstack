info:
  name: update
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8050/profile/3
  body:
    type: json
    data: |-
      {
        "name": "updated"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
