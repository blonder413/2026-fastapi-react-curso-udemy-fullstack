info:
  name: create
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8050/state
  body:
    type: json
    data: |-
      {
        "nombre": "new state"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
