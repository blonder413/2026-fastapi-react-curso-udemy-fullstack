info:
  name: update
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8050/state/6
  body:
    type: json
    data: |-
      {
        "nombre": "available"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
