info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8050/auth/login
  body:
    type: json
    data: |-
      {
        "email": "jvalentine@bsaa.org",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
