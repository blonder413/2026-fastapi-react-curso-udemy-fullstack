info:
  name: jsonBody
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8050/ejemplo/json-body
  body:
    type: json
    data: |-
      {
        "name":"Joe Doe",
        "description":"good man",
        "alive":true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
