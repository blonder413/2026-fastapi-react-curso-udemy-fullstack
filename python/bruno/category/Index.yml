info:
  name: Index
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8050/category
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
