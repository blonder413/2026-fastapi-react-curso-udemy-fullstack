info:
  name: create
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8050/category
  body:
    type: json
    data: |-
      {
        "nombre": "Nueva categoría"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
